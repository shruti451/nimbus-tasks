services:

  jenkins:
    image: jenkins/jenkins:lts-jdk21
    container_name: nimbus-jenkins
    ports:
      - "8081:8080"
    
    volumes:
      - jenkins_home:/var/jenkins_home

volumes:
  jenkins_home:
